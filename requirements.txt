# Autor: Massanori
# Data: 10/06/2026
# Descricao: Dependencias com versoes fixadas (pin) para reprodutibilidade
#            auditavel. Valores alinhados ao ambiente documentado no README
#            (Python 3.12 + PyTorch 2.10 / CUDA 12.8). Para reproducao
#            byte-exata, substituir pela saida de `pip freeze` do ambiente
#            conda `tcc`. As builds CUDA do torch vem do indice oficial do
#            PyTorch (--index-url), nao do PyPI.
fastmri==0.3.0
torch==2.10.0
h5py==3.11.0
numpy==1.26.4
pandas==2.2.2
scipy==1.13.0
scikit-learn==1.4.2
matplotlib==3.8.4
seaborn==0.13.2
tensorboard==2.16.2
